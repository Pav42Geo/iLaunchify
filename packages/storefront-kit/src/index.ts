// Storefront template components. V1 ships ONE template, themed by Brand.
//
// Components (added across Weeks 9-10 per ROADMAP.md):
//   <Hero /> — full-width hero with brand image + tagline
//   <BrandAbout /> — about paragraph block
//   <ProductGrid /> — products grid with featured ordering
//   <ProductCard /> — single product card
//   <StorefrontHeader /> — sticky header with logo + cart
//   <StorefrontFooter /> — auto-policies + social links
//   <PoliciesPage /> — auto-generated privacy/terms/shipping/returns
//
// Theming: all components read from CSS variables set by the app root layout
// (see packages/ui/src/brand-theme.ts).

export const STOREFRONT_KIT_VERSION = '0.1.0'
// Components added in Weeks 9-10.
