// First-run landing removed (Pavel 2026-07-14) — the Products page renders
// its normal chrome with the built-in empty state instead. File kept only
// because the sandbox cannot delete it — remove via git rm in the commit.
export {}
