// Address region data — re-exported from @ilaunchify/types (promoted there
// 2026-07-12 so the creator app's Brand legal-identity form shares the same
// lists). Country OPTIONS still come from platform Markets management
// (@ilaunchify/db getActiveMarketCountries).

export {
  US_STATES,
  CA_PROVINCES,
  regionsForCountry,
  type AddressRegion,
} from '@ilaunchify/types'
