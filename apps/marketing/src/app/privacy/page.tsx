import { LegalDocument } from '@/components/LegalDocument'

export const metadata = { title: 'Privacy Policy — iLaunchify' }

export default function Page() {
  return <LegalDocument slug="privacy" />
}
