import { LegalDocument } from '@/components/LegalDocument'

export const metadata = { title: 'Terms of Service — iLaunchify' }

export default function Page() {
  return <LegalDocument slug="terms" />
}
