'use client'

// Design Studio InfoTip — now re-exported from the shared @ilaunchify/ui
// component so the Studio, Packaging Studio, and Add-Product builder all use the
// one canonical tooltip. Kept as a local re-export so existing import paths in
// the canvas drawers keep working.

export { InfoTip } from '@ilaunchify/ui'
