# Legal Document Management System (Admin → Settings → Legal)

**Spec:** `docs/LEGAL_DOCUMENT_MANAGEMENT_SPEC_2026-07-11.md` (start-here §0). **Status:** Phase L0 substrate built 2026-07-11 (Cowork); gates on `db:push` + `db:generate`.

## Why / thesis
Legal content today is static, duplicated (Terms/Privacy exist as TWO divergent hardcoded copies: marketing `content.ts` + partner `legal-docs.ts`), and creator signup/checkout consent is **client-only, never persisted** (biggest liability hole). Only the Partner Agreement is DB-backed + versioned + e-signed. Solution: generalize that proven pattern into a polymorphic Legal CMS.

## Pavel decisions (2026-07-11)
- Deliver spec first, then build. Consent model = **version-tracked + FORCED re-acceptance on MATERIAL change**, per-document.
- Authoring = **in-app rich editor + uploaded official files (PDF/DOCX/HTML) per version**.
- §9 locked defaults: (1) **soft grace + immediate write-block** (read-only after effectiveAt, never interrupt mid-checkout); (2) dedicated **`legal` notification category**, mandatory (`optOutable:false`); (3) **admins-only publish** via new `legal:publish` capability (ops may draft); (4) **editor-mirrors-file attestation** at publish; (5) **30-day advance notice** default for material privacy/terms; (6) author-but-don't-publish counsel-gated docs (Anti-Circumvention, DPA) until counsel blesses D7 — Accessibility/Cookie/Refund may publish normally.

## Data model (schema.prisma, additive, Cockroach-safe)
`LegalDocument` (slug identity + `currentVersionId` scalar pointer) → `LegalDocumentVersion` (immutable; `bodyHtml`/`bodyText`/`contentSha256`, `changeType` MATERIAL|MINOR, `announceAt`/`effectiveAt`) → `LegalDocumentFile` (per-version uploads) + `LegalAcceptance` (per-user consent ledger, ESIGN/UETA evidence, `@@unique([userId, documentVersionId])`). Enums: LegalDocKind/Audience/VersionStatus/ChangeType/FileFormat/ActorType. Do NOT drop `PartnerAgreement`/`PartnerAgreementSignature` — migrate additively.

## packages/legal (BUILT, Prisma-free, 10 vitest tests green)
Generalizes `apps/partner/src/lib/agreement-signature.ts` (the code comment asked for this). Exports: `sha256Hex`/`canonicalJson`, `buildAcceptanceRecord`/`verifyAcceptanceRecord`, `getPublishedLegalDocument(prismaLike, slug)` (DI'd → single source of truth for live rendering, kills the duplication in L2).

## Phases
L0 substrate (schema + packages/legal + `seed:legal` identities) = DONE + committed (ee2f5e22): `db:push`→`db:generate`→seed ran; 7 identities seeded.
**L1 admin surface = BUILT 2026-07-11** (draft-only, no publish side-effects). New `apps/admin/src/app/(dashboard)/settings/legal/`: `page.tsx` (v2 list — AdminPageHeader + KpiWidget strip + kind/audience chips + table + `LegalRowActions`), `[slug]/page.tsx` + `[slug]/LegalDocumentDetail.tsx` (Tabs: Editor HTML+live-preview / Files [upload deferred to asset pipeline] / Versions / Acceptances / Settings), `actions.ts` (`saveDraftVersion` DRAFT-only guard, `createDraftVersion`, `updateDocumentSettings` — all logAuditAs). Sidebar: `Legal` item (icon Scale) added after Partner Agreements in `sidebar-config.ts`, `capability:'platform:admin'`. contentSha256 computed inline (mirrors @ilaunchify/legal sha256Hex) to avoid a build-config change; wire the pkg dep in L2/L3.
**L2 publish + live rendering = BUILT 2026-07-11.** Admin `actions.ts`: `publishVersion({versionId,changeType MATERIAL|MINOR,effectiveAt,attestMatchesFile})` — $transaction archives prior live → sets version PUBLISHED (publishedAt/By, changeType, effectiveAt) → points `LegalDocument.currentVersionId`; requires attestation (§9-4); logAudit `LEGAL_VERSION_PUBLISHED`; still `platform:admin` (L5 → `legal:publish`). New **Publish tab** in LegalDocumentDetail (Material/Minor radio, effectiveAt datetime, attestation checkbox, material warning). `@ilaunchify/legal` wired into **marketing** only (package.json dep + next.config transpile) — NEEDS `pnpm install`. `apps/marketing/src/lib/legal.ts` `getLiveLegalDoc()` + `LegalDocument.tsx` now **async, DB-first with fallback to legacy content.ts** (draft banner only shown on fallback); 6 slug route wrappers set `force-dynamic`. `getPublishedLegalDocument` DI interface uses `PromiseLike` returns so raw prisma is assignable (verify on Pavel's typecheck post-install).
**L2b partner footer dedup = DONE 2026-07-11.** `SiteFooter` (client) now takes optional `termsHtml`/`privacyHtml`; new async `SiteFooterServer.tsx` fetches published terms/privacy via `getPublishedLegalDocument` and passes them (null → falls back to hardcoded `legal-docs.ts` paras with draft banner). 3 mount points swapped to `<SiteFooterServer/>` (activation page, onboarding layout, partners/apply). `@ilaunchify/legal` wired into partner (dep + transpile) — NEEDS `pnpm install` (same install covers marketing).
**L2 REMAINING (optional):** backfill script to load real content.ts/membership/PartnerAgreement bodies into the draft versions (currently v0.1-draft placeholders; renderer fallback covers pages meanwhile). Publishing pending-counsel drafts is intentionally manual.
**L3 re-acceptance gate = BUILT 2026-07-11 (needs `pnpm install`).** Shared engine `packages/auth/src/legal-gate.ts`: `getOutstandingLegalDocs(userId, role)` (active + requiresAcceptance + audience[role,ALL] + currentVersion published & effective & not yet accepted) and `recordLegalAcceptances(user, versionIds, ip, ua)` (buildAcceptanceRecord evidence → LegalAcceptance rows, idempotent via @@unique, + logAudit `LEGAL_ACCEPTED`). Exported from auth barrel. Per-app: `(dashboard)/LegalGate.tsx` (blocking interstitial, read-per-doc, single agree checkbox, router.refresh) + `legal-gate-actions.ts` (headers ip/ua → recordLegalAcceptances) in BOTH creator & partner; mounted in each `(dashboard)/layout.tsx` (creator right after requireRole; partner AFTER onboarding/status redirects). **The gate is also the new-user capture point** (first dashboard load gates Terms/Privacy once published) — so signup-form capture is optional. **Dep fan-out:** auth now imports @ilaunchify/legal, so ALL auth consumers transpile it — added @ilaunchify/legal dep+transpile to creator & admin (partner+marketing already had it). Verified: new prisma usage type-checks vs generated client (probe); inert until docs published.
**L3 REMAINING/notes:** gate is a HARD block (not the soft-grace-dismiss from §9-1) — grace-window dismiss (cookie + write-gating) is a follow-up; `effectiveAt` already returned in OutstandingLegalDoc for it. Optional: order-time Terms snapshot in checkout `cart-actions.ts placeOrderFromCheckoutDraft` (binds acceptance to the order).
**L4 legal-update email = BUILT 2026-07-11 (needs `db:push`+`db:generate` BEFORE typecheck — new enum value).** Added `LEGAL_DOCUMENT_UPDATED` to `NotificationEvent` (schema) + new mandatory **`legal` category** (`optOutable:false`, both channels) in `center-types.ts` union + `categories.ts` NOTIFICATION_CATEGORIES + `EVENT_CATEGORY`. Registered the event in all 4 compiler-forced maps (categories EVENT_CATEGORY, templates.ts TemplateData+renderTemplate case, template-tokens EVENT_TOKEN_PALETTE, payload-required). Fan-out: `publishVersion` (admin actions.ts) on `changeType==='MATERIAL'` resolves audience→users (CREATOR/PARTNER/ALL→both/PUBLIC→both) and batches `dispatchNotification` (best-effort, never fails publish; immediate = no digest flag; mandatory category bypasses opt-outs). Deep link = `${NEXT_PUBLIC_MARKETING_URL}/${slug}` (relative fallback). admin already deps @ilaunchify/notifications. NOTE: compiler-forced maps only compile AFTER db:generate adds the enum value — push+generate first. Quiet-hours still defers (not skips) mandatory email — known caveat, no bypass built.
**L5 (partial) = BUILT 2026-07-11.** (a) `legal:publish` capability added to `capability-rules.ts` (Capability union + ALL_CAPABILITIES; SUPER_ADMIN auto-holds via `'*'`, no migration); `publishVersion` now `requireCapability('legal:publish')` while draft actions stay `platform:admin` (ops draft, admins publish — §9-3). (b) `seed-legal.ts` extended: added identities `acceptable-use`, `refund-dispute-policy`, `subprocessors`, `dpa`; **authored real DRAFT bodies for `accessibility` (WCAG 2.1/2.2 AA statement — Pavel's explicit ask) + `cookie-policy`** via BODY_OVERRIDES with idempotent backfill (re-run `seed:legal` fills placeholder drafts). All DRAFT, unpublished. Re-run: `pnpm --filter @ilaunchify/db seed:legal`.
**Professional build-accurate draft content authored for ALL 11 docs = 2026-07-11.** `packages/db/prisma/legal-content.ts` `LEGAL_BODIES` (Terms, Privacy, Cookie, Creator Agreement, Partner Agreement summary, Membership, Accessibility, Acceptable Use, Refund/Dispute, Sub-processors, DPA) — written to reflect the actual build (tiers Maker/Builder/Agency + production fee at checkout, earned merit fee withheld from partner payout, Stripe Connect/Billing, creator owns brand/claims/FDA, orchestration, on-platform/anti-circumvention, US-only V1, sub-processors incl. Stripe/EasyPost, tenant isolation). All clearly marked pre-counsel DRAFTS. `seed-legal.ts` now imports LEGAL_BODIES as BODY_OVERRIDES → re-running `seed:legal` backfills all into the CMS draft versions (idempotent; only replaces placeholder drafts). tsc-clean. Cookie route + content module also added (mirrors accessibility). Also authored /cookie-policy public route + footer link.
**/accessibility public route = BUILT 2026-07-11.** `apps/marketing/src/app/accessibility/page.tsx` (force-dynamic, noindex) renders `<LegalDocument slug="accessibility" doc={ACCESSIBILITY_STATEMENT}/>`; content module `content/legal/accessibility.ts`; footer link added to LandingFooter Company col. **Renderer refactored to DB-first fallback:** `doc` prop is now a draft FALLBACK (not unconditional override) — published DB version always wins, else `doc`, else LEGAL_DOCS[slug]. So /accessibility shows the draft (banner) now and auto-switches to the published version. (Same pattern reusable for cookie-policy/refund routes.)
**L5 REMAINING:** public routes for the other new docs (cookie/refund/AUP/subprocessors/DPA — mirror /accessibility); file-upload UI (needs asset pipeline); WYSIWYG editor (L1 is HTML-source textarea); soft-grace dismiss (effectiveAt already returned); order-time checkout Terms snapshot; quiet-hours bypass for mandatory legal email.

## Gotchas
- Adding the L4 `LEGAL_DOCUMENT_UPDATED` NotificationEvent forces updating the compiler-checked `EVENT_CATEGORY` map in `packages/notifications/categories.ts` (deferred to L4 on purpose).
- Seed only creates identities + `v0.1-draft` placeholders; real bodies backfilled in L2 from `content.ts`/`membership-terms.ts`/`PartnerAgreement`. Nothing published, so no live page changes from L0.
